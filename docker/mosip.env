# MOSIP Database Configuration
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mosip@123
POSTGRES_PORT=5432

# Database Names
MOSIP_MASTER_DB=mosip_master
MOSIP_KERNEL_DB=mosip_kernel
MOSIP_IAM_DB=mosip_iam
MOSIP_IDA_DB=mosip_ida
MOSIP_IDREPO_DB=mosip_idrepo
MOSIP_IDMAP_DB=mosip_idmap
MOSIP_PREREG_DB=mosip_prereg
MOSIP_REG_DB=mosip_reg
MOSIP_REGPRC_DB=mosip_regprc
MOSIP_AUDIT_DB=mosip_audit
MOSIP_PMP_DB=mosip_pmp

# User Credentials
SYSADMIN_USER=sysadmin
SYSADMIN_PASSWORD=Mosipadm@dev123
DBADMIN_PASSWORD=Mosipadm@dev123
APPADMIN_PASSWORD=Mosipadm@dev123
DBUSER_PASSWORD=Mosip@dev123

# Replication User
REPLICATION_USER=replicator
REPLICATION_PASSWORD=replicator123

# Paths
BASE_PATH=/database
LOG_PATH=/var/log/mosip

# pgAdmin Configuration
PGADMIN_EMAIL=admin@mosip.local
PGADMIN_PASSWORD=admin123
PGADMIN_PORT=8080

# Redis Configuration (dev-advanced profile)
REDIS_PORT=6379

# Resource Limits
POSTGRES_MEMORY_LIMIT=2G
POSTGRES_CPU_LIMIT=1.0
POSTGRES_MEMORY_RESERVATION=1G
POSTGRES_CPU_RESERVATION=0.5

# Network Configuration
POSTGRES_NETWORK=172.20.0.0/16