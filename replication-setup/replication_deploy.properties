# Replication deployment properties
# Database connection
DB_SERVERIP=localhost
DB_PORT=5433
SU_USER=postgres
SU_USER_PWD=mosip@123
DEFAULT_DB_NAME=postgres

# Target database and schema (for initial empty publication)
TARGET_DB_NAME=
TARGET_SCHEMA=

# Replication user (publication owner)
REPLICATION_USER=postgres
REPLICATION_PWD=mosip@123

# Publication
PUBLICATION_NAME=

# Add tables (comma-separated, e.g., iam.user_detail, iam.user_role)
TABLE_LIST=

# Logging
LOG_PATH=/tmp/replication-setup/
